import 'package:flutter/material.dart';

class PathsPage extends StatelessWidget {
  static const route = "/paths";

  const PathsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Paths Page");
  }
}
